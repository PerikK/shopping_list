export default function Item({item}) {
    return (
        <>
            return (
            <li>
                <span>
                    {item.qty} {item.name}
                </span>
                <button>❌</button>
            </li>
        </>
    )
}